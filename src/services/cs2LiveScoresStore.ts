/**
 * CS2 Live Scores Store — re-exported from unified LiveScoresStore.
 *
 * Kept as separate file for backward-compatible imports.
 * The actual implementation lives in liveScoresStore.ts.
 */

export { cs2LiveScoresStore } from './liveScoresStore';
export type { LiveScoreState } from './liveScoresStore';
